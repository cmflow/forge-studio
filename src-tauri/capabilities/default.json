{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for forge-studio",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "dialog:default",
    "dialog:allow-open",
    "fs:default",
    "opener:default",
    "shell:allow-open"
  ]
}
